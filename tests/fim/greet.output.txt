# case: greet   profile: qwen2.5-coder   mode: infill   endpoint: http://127.0.0.1:8012
# deps: greet_deps.py

=== prompt without deps ===
<|repo_name|>myproject
<|file_sep|>filename
<|fim_prefix|>from greet_deps import Greeter


def main() -> None:
    g = Greeter("world")
    print(g.<|fim_suffix|>)


if __name__ == "__main__":
    main()<|fim_middle|>

=== prompt with greet_deps.py ===
<|repo_name|>myproject
<|file_sep|>greet_deps.py
class Greeter:
    def __init__(self, name: str) -> None:
        self.name = name

    def greeting(self) -> str:
        return f"Hello, {self.name}!"

    def shout(self) -> str:
        return self.greeting().upper()<|file_sep|>filename
<|fim_prefix|>from greet_deps import Greeter


def main() -> None:
    g = Greeter("world")
    print(g.<|fim_suffix|>)


if __name__ == "__main__":
    main()<|fim_middle|>

=== completion without deps ===
greet()

=== completion with greet_deps.py ===
greeting()
